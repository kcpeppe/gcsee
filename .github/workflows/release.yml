name: Release to Maven Central

on:
  workflow_dispatch:
    inputs:
      release_version:
        description: 'Release version (e.g. 0.1.0)'
        required: true
      next_version:
        description: 'Next development version (e.g. 0.2.0-SNAPSHOT)'
        required: true
        default: ''
      force:
        description: 'Allow overwrite of existing release/tag (true/false)'
        required: true
        default: 'true'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: 'maven'

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # -----------------------------------------------------------------------
      # Guard: Fail early unless force=true
      # -----------------------------------------------------------------------
      - name: Check if tag already exists (guard)
        if: ${{ github.event.inputs.force != 'true' }}
        run: |
          if git ls-remote --tags origin | grep -q "refs/tags/v${{ github.event.inputs.release_version }}"; then
            echo "Tag v${{ github.event.inputs.release_version }} already exists on remote."
            echo "Re-run with force=true to overwrite, or bump the version."
            exit 1
          fi

      - name: Write Maven settings.xml
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml <<SETTINGS
          <settings>
            <servers>
              <server>
                <id>central</id>
                <username>${{ secrets.MAVENCENTRAL_USERNAME }}</username>
                <password>${{ secrets.MAVENCENTRAL_PASSWORD }}</password>
              </server>
            </servers>
          </settings>
          SETTINGS

      - name: Set release version
        run: ./mvnw -B versions:set -DnewVersion=${{ github.event.inputs.release_version }} -DgenerateBackupPoms=false

      - name: Commit release version
        run: |
          git add -A
          git commit -m "Release ${{ github.event.inputs.release_version }}"

      - name: Build and stage artifacts
        run: ./mvnw -B -Prelease deploy -Dgpg.skip=true

      # -----------------------------------------------------------------------
      # JReleaser with conditional overwrite
      # -----------------------------------------------------------------------
      - name: JReleaser full-release
        run: ./mvnw -B -Pjreleaser jreleaser:full-release
        env:
          JRELEASER_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          JRELEASER_GPG_PASSPHRASE: ${{ secrets.JRELEASER_GPG_PASSPHRASE }}
          JRELEASER_GPG_SECRET_KEY: ${{ secrets.JRELEASER_GPG_SECRET_KEY }}
          JRELEASER_GPG_PUBLIC_KEY: ${{ secrets.JRELEASER_GPG_PUBLIC_KEY }}
          JRELEASER_MAVENCENTRAL_USERNAME: ${{ secrets.MAVENCENTRAL_USERNAME }}
          JRELEASER_MAVENCENTRAL_PASSWORD: ${{ secrets.MAVENCENTRAL_PASSWORD }}
          JRELEASER_RELEASE_GITHUB_OVERWRITE: ${{ github.event.inputs.force }}

      - name: Set next development version
        run: ./mvnw -B versions:set -DnewVersion=${{ github.event.inputs.next_version }} -DgenerateBackupPoms=false

      - name: Commit and push next version
        run: |
          git add -A
          git commit -m "Prepare next development version ${{ github.event.inputs.next_version }}"
          git push origin main

      - name: Upload JReleaser output
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: jreleaser-output
          path: |
            target/jreleaser/trace.log
            target/jreleaser/output.properties

